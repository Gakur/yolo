---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "yolocluster1"
  namespace: "default"
  labels:
    app: "yolocluster1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: "yolocluster1"
  template:
    metadata:
      labels:
        app: "yolocluster1"
    spec:
      containers:
      - name: "yolo-1"
        image: "gcr.io/yoloproject1-375909/github.com/gakur/yolo:$SHORT_SHA"
      - name: "yolo-2"
        image: "gcr.io/yoloproject1-375909/github.com/gakur/yolo:$SHORT_SHA"
---
apiVersion: "autoscaling/v2"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "yolocluster1-hpa-vkuh"
  namespace: "default"
  labels:
    app: "yolocluster1"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "yolocluster1"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: "Resource"
    resource:
      name: "cpu"
      target:
        type: "Utilization"
        averageUtilization: 80
